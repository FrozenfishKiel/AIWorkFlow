# 第一天环境清理与项目开工计划

## 一、这份计划现在怎么用

这份文档不再只是“待执行清单”，而是今天这轮工作的实际状态同步。

它回答两个问题：

1. 哪些第一天事项已经完成。
2. 哪些仍然属于待确认、待安装、待验证的系统级准备。

## 二、今天已经完成的部分

### 2.1 清理

- [x] 删除旧 conda 环境：
  - `arknight-ai`
  - `maa-auto`
  - `x-anylabeling`
  - `x-anylabeling-cpu`
- [x] 删除孤立目录：
  - `good-job-ue`
  - `_dll_backup_20260611_0115`
- [x] 执行 `conda clean --all -y`
- [x] 回收 `D:` 盘空间，为项目环境腾出余量

### 2.2 项目 Python 环境

- [x] 创建项目环境 `D:\Anaconda3\envs\ai-content-ops`
- [x] 确认 Python 版本 `3.12.13`
- [x] 安装 `apps/api/requirements-dev.txt`
- [x] 验证 `fastapi / uvicorn / redis / celery` 等关键包已安装

### 2.3 仓库骨架

- [x] 建立 `apps/api`
- [x] 建立 `apps/web`
- [x] 建立 `infra`
- [x] 建立 `tests`
- [x] 建立 `scripts`
- [x] 建立文档目录和索引结构

### 2.4 最小运行验证

- [x] 后端 `/health` 请求返回 `200`
- [x] 前端 `npm run build` 成功
- [x] `infra/docker-compose.yml` 已具备 PostgreSQL / Redis / MinIO 草案

## 三、今天仍待进入方案阶段确认的部分

这些不是环境阻塞项，而是已经收敛到技术方案阶段需要确认的设计项。

### 3.1 Docker 与 Windows 前置条件

- [ ] 是否后续还要额外补 `WSL` 路线，不作为当前阶段阻塞项
- [x] 确认 `VirtualMachinePlatform` 与 `Hyper-V` 可支撑当前方案
- [x] 确认 Docker Desktop 已安装并可运行
- [x] 确认 Docker 当前可满足“不新增重压力到 C 盘”的要求

### 3.2 基础服务落地

- [x] 创建当前实际使用的数据目录 `D:\ai-content-ops\minio-data`
- [x] 启动 `PostgreSQL`
- [x] 启动 `Redis`
- [x] 启动 `MinIO`
- [x] 验证 `5432 / 6379 / 9000 / 9001` 端口连通
- [x] 验证 PostgreSQL 最小查询 / Redis `PING` / MinIO 健康检查
- [x] 确认 PostgreSQL 在本机必须使用 Docker named volume，而不是 Windows 宿主绑定目录

### 3.3 Celery 运行形态

- [ ] 确认 Celery 第一阶段是否只保留应用内 worker 入口
- [ ] 或确认它是否要在 compose 里补服务定义

## 四、今天这轮工作的关键判断

### 4.1 已经可以继续的

下面这些事，已经不需要再等待系统层安装：

- 继续整理技术方案
- 继续细化模块边界
- 继续拆阶段实施顺序
- 继续准备 review 规范和测试策略
- 继续在项目环境里扩展前后端骨架

### 4.2 仍然要谨慎的

下面这些事不再是环境未完成，而是方案阶段必须定掉：

- Celery 最终接入方式
- 默认 Python 链路误用风险
- 开发态默认凭据与镜像版本策略

## 五、第一天之后的建议顺序

最合理的继续顺序如下：

1. 先把 Celery、Redis、MinIO 的方案边界定掉。
2. 把当前基础服务落盘策略写进正式技术方案。
3. 补运行说明、README 和后续脚本约束。
4. 然后进入正式技术方案和功能实施拆解。

## 六、当前状态一句话总结

第一天的“项目内准备”和“系统级最小准备”都已完成，当前已经可以正式进入技术方案编写与实施拆解阶段。
