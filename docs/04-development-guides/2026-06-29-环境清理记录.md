# 环境清理记录

## 清理前磁盘空间

| 盘符 | 可用空间 |
| --- | ---: |
| `C:` | 1.49 GB |
| `D:` | 17.77 GB |
| `E:` | 139.26 GB |

## 清理前 Conda 环境

- `base`
- `arknight-ai`
- `maa-auto`
- `x-anylabeling`
- `x-anylabeling-cpu`

## 发现但未注册到 conda env list 的目录

- `good-job-ue`
- `_dll_backup_20260611_0115`

## 待删除环境

- `arknight-ai`，约 `6.42 GB`
- `maa-auto`，约 `1.67 GB`
- `x-anylabeling-cpu`，约 `0.93 GB`
- `x-anylabeling`，约 `0.14 GB`
- `good-job-ue`，约 `0.04 GB`
- `_dll_backup_20260611_0115`，约 `0 GB`

## 计划清理项

- 删除无用的 `conda` 环境
- 删除未注册但无用的环境目录
- 清理 `D:\Anaconda3\pkgs` 缓存

## 目标

- 回收 `D:` 盘空间
- 为 `D:\Anaconda3\envs\ai-content-ops` 预留空间
- 降低旧项目环境对本项目的干扰

## 清理后结果

| 盘符 | 可用空间 |
| --- | ---: |
| `C:` | 1.49 GB |
| `D:` | 36.68 GB |
| `E:` | 139.26 GB |

## 清理与创建结果

- 已删除全部旧项目环境目录
- 已清理 `conda` 缓存
- 已创建 `D:\Anaconda3\envs\ai-content-ops`
- 当前项目 Python 环境版本为 `3.12.13`
