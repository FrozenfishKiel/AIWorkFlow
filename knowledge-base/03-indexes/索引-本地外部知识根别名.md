# 索引：本地外部知识根别名

## 目的

这份索引只说明“当前项目知识库里提到的外部知识根别名，对应本机什么路径”。

这样可以减少把绝对路径反复写进知识卡片里的次数。

## 当前别名

- `game_ai_dev`
  - `E:\Game\AI Development`

## 维护规则

- 如果本地路径变了，优先只更新这里和对应根入口卡片。
- 其他卡片尽量只写 `source_root_alias + source_relpath`，不要重复写整条绝对路径。
